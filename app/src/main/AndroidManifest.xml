<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.SeeCategory"></activity>
        <activity android:name=".UI.MySeeBill" />
        <activity android:name=".UI.SeeBook" />
        <activity android:name=".UI.Main2ActivityPass" />
        <activity android:name=".UI.MySeeUser" />
        <activity android:name=".UI.CreateAcc" />
        <activity android:name=".UI.StatisticsActivity" />
        <activity android:name=".UI.BillActivity" />
        <activity android:name=".UI.TopActivity" />
        <activity android:name=".UI.CategoryActivity" />
        <activity android:name=".UI.BookActivity" />
        <activity android:name=".UI.UserActivity" />
        <activity android:name=".UI.HomeActivity" />
        <activity android:name=".UI.LoginActivity" />
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>